apiVersion: autoscaling.agones.dev/v1
kind: FleetAutoscaler
metadata:
  name: parkourtag
  namespace: emortalmc

spec:
  fleetName: parkourtag
  policy:
    type: Buffer
    buffer:
      bufferSize: 1
      minReplicas: 1
      maxReplicas: 5
